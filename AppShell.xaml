<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ReturnToMonkee.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="ReturnToMonkee">

    <ShellContent
        x:Name="HomeShellContent"
        Title="Home"
        Route="MainPage" />
    <ShellContent
        x:Name="TestStringCrudShellContent"
        Title="TestString CRUD"
        Route="TestStringCrudPage" />
    <ShellContent
        x:Name="BewegungsErinnerungShellContent"
        Title="Bewegungserinnerungen"
        Route="BewegungsErinnerungPage" />

</Shell>
